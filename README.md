# Headie — Portfolio

Cinematic personal portfolio for **Headie** (`@iamheadie`).

Live: https://headie-portfolio.vercel.app

```bash
npm install
npm run dev
```

Add videos to `public/videos/` and posters to `public/images/`.
Edit `src/lib/projects.ts` and `src/lib/site.ts`.
